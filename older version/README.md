# older version

This folder archives superseded public releases of Metis, verbatim, the same way the Phanes repository keeps its `older version/` tree.

v0.2 is the first public release, so there is nothing here yet. When a later version ships, the outgoing one is copied into a dated subfolder here before the top-level files are updated, so every published version stays recoverable.
